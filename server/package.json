{
  "name": "secret-voldemort-server",
  "version": "1.0.0",
  "description": "secret voldemort websocket server",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ramiro Zapata",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.1.0",
    "firebase-admin": "^6.1.0",
    "socket.io": "^2.1.1"
  }
}
